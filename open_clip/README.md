# open_clip (vendored)

Vendored copy of the public **OpenCLIP** library, used to load the frozen
BiomedCLIP backbone (imported as `open_clip.src.open_clip`). See `LICENSE` for the
original license and copyright.
